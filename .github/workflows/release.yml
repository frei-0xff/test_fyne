on: push
name: Build Release
jobs:
  release-linux-amd64:
    name: release linux/amd64
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: compile and release
      uses: fyneio/fyne-cross:base-latest
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
